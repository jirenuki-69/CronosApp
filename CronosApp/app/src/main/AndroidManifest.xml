<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cronos.com.cronosapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AsistentesActivity"></activity>
        <activity android:name=".ClasesActivity" />
        <activity android:name=".ActualizarMateriaActivity" />
        <activity android:name=".GenerateQRActivity" />
        <activity android:name=".GruposActivity" />
        <activity android:name=".MateriaActivity" />
        <activity android:name=".HomeAlumnoActivity" />
        <activity android:name=".HomeProfesorActivity" />
        <activity android:name=".RegistroProfesorActivity" />
        <activity android:name=".RegistroAlumnoActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".SelectProfile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>